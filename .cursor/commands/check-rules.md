---
description: Check which project rules are currently active
---

List all project rules you can see right now from `.cursor/rules/` directory.

For each rule:
1. Show the file path
2. Show the `globs` pattern (if defined)
3. Briefly summarize what the rule requires

Confirm which rules will apply to my next request based on the current file context.
